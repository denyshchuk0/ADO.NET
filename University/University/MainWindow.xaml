<Window x:Class="University.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="306*"/>
            <RowDefinition Height="113*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="395*"/>
            <ColumnDefinition Width="397*"/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding}" Name="dgStudent"/>
        <DataGrid Grid.Column="1" ItemsSource="{Binding}" Name="dgGroup"/>
        <StackPanel Grid.Row="2" >
            <TextBox x:Name="tbName"/>
            <TextBox x:Name="tbSurname"/>
            <TextBox x:Name="tbGroup"/>
            <Button  x:Name="btnCreate" Content="create" Click="btnCreate_Click"/>
            <Button  x:Name="btnDelete" Content="delete" Click="btnDelete_Click"/>

        </StackPanel>
    </Grid>
</Window>
